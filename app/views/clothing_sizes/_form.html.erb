<%= form_for(@clothing_size) do |f| %>
  <% if @clothing_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clothing_size.errors.count, "error") %> prohibited this clothing_size from being saved:</h2>

      <ul>
      <% @clothing_size.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sizes %><br>
    <%= f.text_field :sizes %>
  </div>
  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
